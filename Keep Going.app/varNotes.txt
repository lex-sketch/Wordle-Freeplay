    ContentView()
        // Match the real app setup so the preview can render views that expect
        // the shared tracker from the SwiftUI environment.
        .environmentObject(DayCounterTracker.shared)
        .environmentObject(goalMaker.shared2)
        
        ## environmentObject - stores the object/thing for later recall between views
        
    example:
        struct goalsWelcome: View {
    @Binding var currentGoalsT: Int
    @EnvironmentObject var tracker: DayCounterTracker
    @EnvironmentObject var goalTracker: goalMaker
